#pragma once

constexpr unsigned char HEIGHT = 20;
constexpr unsigned char WIDTH = 16;

constexpr unsigned char SCALE = 30;

void gameOver_Screen(sf::RenderWindow& window);
void check_Endgame();
